# Never bake local state or secrets into images
node_modules/
.next/
.env
.env.*
test-results/
playwright-report/
e2e/
# Dev tooling only: the docs screenshot capture imports ../e2e/helpers, which
# is excluded above, so leaving scripts/ in would fail the image's type check.
scripts/
