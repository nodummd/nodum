"""Placeholder for future command groups.

Phase 1 is the compose wrapper only (nodum stack ...). When the API client
lands it will live in its own module here, e.g. ``commands/api.py``, so the
top-level ``nodum`` verb space stays free.
"""

from __future__ import annotations
